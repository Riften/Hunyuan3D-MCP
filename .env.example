# Documentation only: the server does not automatically load .env files.
HY3D_API_KEY=
# Optional HTTP proxy; standard HTTPS_PROXY / HTTP_PROXY are also supported.
# HY3D_PROXY=http://127.0.0.1:7897
# HY3D_TIMEOUT_SECONDS=30
